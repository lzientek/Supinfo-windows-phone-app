<Page
    x:Class="SupinfoNote.Uni10.Views.ConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SupinfoNote.Uni10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding Source={StaticResource Locator},Path=Connection}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="30" HorizontalAlignment="Center">
            <TextBlock Text="Campus ID" />
            <TextBox Text="{Binding CampusId}" />
            <TextBlock Text="Password" />
            <PasswordBox Password="{Binding Password}" />
            <Button Content="Login" Command="{Binding ConnectCommand}" />
        </StackPanel>
    </Grid>
</Page>
